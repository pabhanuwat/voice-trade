const express = require('express')
const router = express.Router();

router.route('/register')
    .get((req, res) => {

    })

router.route('/login')
    .get((req, res) =>{

    })

router.route('/logout')
    .get((req,res) => {

    })

module.exports = router;